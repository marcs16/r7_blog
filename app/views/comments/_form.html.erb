<%= form_with(model: [post, comment], submit_label: submit_label) do |f| %>
    <div class="form-control">
        <%= f.rich_text_area :body %>
        <%= f.submit submit_label, class:"btn btn-outline-primary mt-1" %>
    </div>
<% end %>
