<div id="<%= dom_id post %>">
  <h2>
    <%= post.title %>
  </h2>
  <h3>Posted by: <%= link_to post.user.name, user_path(post.user) %></h3>
  <h6><%= pluralize( post.views, "view") %></h6>
  <div>
    <%= post.body %>
  </div>

</div>
